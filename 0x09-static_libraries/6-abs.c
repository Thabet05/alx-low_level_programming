#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @c: the input integre
 * Return: +@c or -@c
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	return (-c);
}

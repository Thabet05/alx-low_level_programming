#include "main.h"
/**
 *
 */
{
	if()
		{
			exit(97);
			write(STDOUT_FILENO, string, nmbrbytes);
		}
	fd = open(file_to, O_WRONLY | O_TRUNC);
	if (fd = -1)
	{
		option = open(file_to, O_CREAT | O_WRONLY | O_TRUNC);
		option = write(fd, content, nmbrbytes);
		if (option == -1)
		{
			close(fd);
		}
	}
}

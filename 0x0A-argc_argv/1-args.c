#include "main.h"
/**
 * main - a program that prints the number of arguments followed by a new line
 * @argc: the size of the array argv
 * @argv: the array
 * Return: 0
 */
int main(int argc, char *argv[])
{
	_putchar("%d\n", argc - 1);
	return (0);
}
